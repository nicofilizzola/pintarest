{% extends 'base.html.twig' %}

{#{% block title %}Hello PinController!{% endblock %}#}

{% block body %}
    <article>
        <h1>{{ pin.title }}</h1>
        <p>{{ pin.description }}</p>
    </article>
    <a href="{{ path('app_home') }}">Return</a>
{% endblock %}