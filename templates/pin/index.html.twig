{% extends 'base.html.twig' %}

{#{% block title %}Hello PinController!{% endblock %}#}

{% block body %}
    {% if allPins|length > 0 %}
        <h1>{{ is_plural(allPins|length, 'Pin') }}</h1>

        {% for pin in allPins %}
            <article>
                <h2><a href="{{ path('app_pin_show', {id: pin.id}) }}">{{ pin.title }}</a></h2>
                <p>{{ pin.description }}</p>
            </article>
        {% endfor %}
    {% endif %}
{% endblock %}
