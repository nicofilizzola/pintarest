{% extends 'base.html.twig' %}

{#{% block title %}Hello PinController!{% endblock %}#}

{% block body %}
    {% if allPins|length > 0 %}
        <h1>{{ is_plural(allPins|length, 'Pin') }}</h1>

        {% for pin in allPins %}
            <article>
                <h2>{{ pin.title }}</h2>
                <p>{{ pin.description }}</p>
            </article>
        {% endfor %}
    {% endif %}
{% endblock %}
